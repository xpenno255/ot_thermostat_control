# ot_thermostat_control

## Agent skills

### Issue tracker

Issues live as GitHub issues in [xpenno255/ot_thermostat_control](https://github.com/xpenno255/ot_thermostat_control), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary; each label string equals its role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
